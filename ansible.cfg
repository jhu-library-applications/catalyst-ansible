[defaults]
host_key_checking = False
roles_path = roles
remote_user = deploy
vault_password_file = ~/.ssh/catalyst-ansible/vault_password_file

# dev:
inventory = inventory/vagrant
# stage:
#inventory = inventory/stage

# first-run vars for setup.yml:
#remote_user = your_JHED_here
# localhost or network servers only:
#ask_pass = True
#ask_sudo_pass = True
